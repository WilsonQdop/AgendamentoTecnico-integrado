spring:
  application:
    name: AgendamentoTecnico

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:agendamentos_db}
    username: ${DB_USER:Qdop}
    password: ${DB_PASS:Qdop123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8443
  ssl:
    key-store: classpath:keystore.p12
    key-store-password: WilsonQdop
    key-store-type: PKCS12
    key-alias: backend
    enabled: true
  tomcat:
    connection-timeout: 20000
    max-connections: 10000

api:
  security:
    token:
      secret: ${JWT_SECRET:secret-qdop}

app:
  admin:
    password: ${INITIAL_ADMIN_PASSWORD:senha123!}
    email: ${INITIAL_ADMIN_EMAIL:admin@wilson.com}
